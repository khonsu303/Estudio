# Puerto del servidor
PORT=5000

# MongoDB Connection String
# Opción 1: MongoDB Local
MONGODB_URI=mongodb://localhost:27017/mobile-app

# Opción 2: MongoDB Atlas (Cloud) - Recomendado
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/mobile-app?retryWrites=true&w=majority

# JWT Secret (cambiar por una clave segura en producción)
JWT_SECRET=tu_clave_secreta_super_segura_cambiala_en_produccion

# JWT Expiration
JWT_EXPIRE=7d

# Node Environment
NODE_ENV=development
